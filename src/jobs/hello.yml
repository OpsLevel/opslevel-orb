description: >
  Sample description
# What will this job do?
# TODO: what to do with this file?

executor: default

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - greet:
      to: << parameters.to >>
